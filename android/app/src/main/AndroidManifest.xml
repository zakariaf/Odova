<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- SPEC.md §4. Three permissions, and the two that are ABSENT matter more
         than the three that are here.

         POST_NOTIFICATIONS is Android 13+'s runtime permission, asked once,
         behind §4.4.5's pre-prompt. RECEIVE_BOOT_COMPLETED and the receiver
         below are §6.2's boot re-arm: Android drops every alarm on reboot, so
         without them a phone that restarts overnight goes silent forever.
         VIBRATE is what the notification itself needs.

         NOT declared, and never to be: USE_EXACT_ALARM and
         SCHEDULE_EXACT_ALARM. SPEC.md §4.6.3 — "Do not request exact-alarm
         privileges. Nothing here needs minute precision, and asking is a
         review risk and a trust cost." USE_EXACT_ALARM is restricted by Play
         policy to alarm, timer and calendar apps and would risk rejection;
         SCHEDULE_EXACT_ALARM is grantable but is a permission dialog spent on
         an hour of precision this app has no use for. §4.2's bodies are
         absolute anchors ("due around 12 October") precisely so that about an
         hour of drift costs nothing.

         The `local-notifications-scheduler` skill's own manifest gate REQUIRES
         SCHEDULE_EXACT_ALARM. It is a general default and this is the
         product's decision; CLAUDE.md §3 says the spec wins. Odova therefore
         runs tools/check_notification_manifest.sh instead, which forbids
         both. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:label="Odova"
        android:name="${applicationName}"
        android:localeConfig="@xml/locales_config"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- SPEC.md §12's share hand-off. A FileProvider grants the receiving
             app read access to ONE file for the length of one Intent, which is
             why this app asks for no storage permission at all — §12: "the app
             never asks for storage permission". A `file://` URI would also
             throw FileUriExposedException on API 24 and above.

             `android:exported="false"`: only this app may hand out grants. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- SPEC.md §6.2: Android drops every scheduled alarm on reboot, so
             the queue is rebuilt after the device unlocks. Without this a
             phone that restarts overnight never notifies again and nothing
             says so. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
