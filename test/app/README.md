# `test/app` — the composition root's tests

`bootstrap_test.dart` (the error net and the startup order) and
`providers_test.dart` (the placeholder overrides and the retry policy).

The root widget's locale contract is tested from `test/l10n/` instead, next to
the ARB files and the delegates it is about.
